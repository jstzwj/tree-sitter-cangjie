
main() {
    let brr = Array<Int32>(10, item: 0)
    println(brr)
    if (brr[0] == 0) {
        println("brr[0] is 0")
    }

    let a = 0
    let b = 1
    a < b
    a <= b
    result.append((brr as Array<Int64>).getOrThrow())
    result.append((brr == Array<Int64>).getOrThrow())
}